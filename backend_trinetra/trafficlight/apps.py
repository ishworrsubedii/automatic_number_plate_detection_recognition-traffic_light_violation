from django.apps import AppConfig


class TrafficlightConfig(AppConfig):
    """
    This class is used to configure the trafficlight app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'trafficlight'
